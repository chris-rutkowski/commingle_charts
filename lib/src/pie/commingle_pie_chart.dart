import 'package:flutter/widgets.dart';

class ComminglePieChart extends StatelessWidget {
  const ComminglePieChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('ComminglePieChart');
  }
}
